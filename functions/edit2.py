from functions.Processing import edit1

async def amiri_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/Amiri.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)



async def amiri_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/Amiri.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def rqaa_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/rqaa.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def rqaa_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/rqaa.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def rqaa2_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/rqaa2.ttf"
    color_t = (0, 0, 0)
    size_font = 95
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def rqaa2_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/rqaa2.ttf"
    color_t = (250, 250, 250)
    size_font = 95
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qran_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qraan.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qran_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qraan.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def tbaa_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/tbaa.ttf"
    color_t = (0, 0, 0)
    size_font = 80
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def tbaa_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/tbaa.ttf"
    color_t = (250, 250, 250)
    size_font = 80
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hsha_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hsha.ttf"
    color_t = (0, 0, 0)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hsha_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hsha.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qyass_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qyasee.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qyass_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qyasee.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qyass2_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qyasee2.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def qyass2_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/qyasee2.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hur_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hur.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hur_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hur.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hur2_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hur2.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def hur2_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/hur2.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def alanat_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/alanat.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def alanat_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/alanat.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def alanat2_BXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/add.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)


async def alanat2_WXX(client, message):
    dir_bg = "./bg2"
    dir_font = "./fonts/add.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
