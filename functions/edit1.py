from functions.Processing import edit1
from functions.edit import MOR

async def amiri_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/Amiri.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    codx = "amiri_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def amiri_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/Amiri.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    codx = "amiri_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def rqaa_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/rqaa.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    codx = "rqaa_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def rqaa_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/rqaa.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    codx = "rqaa_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def rqaa2_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/rqaa2.ttf"
    color_t = (0, 0, 0)
    size_font = 95
    codx = "rqaa2_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def rqaa2_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/rqaa2.ttf"
    color_t = (250, 250, 250)
    size_font = 95
    codx = "rqaa2_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qran_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qraan.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    codx = "qran_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qran_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qraan.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    codx = "qran_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def tbaa_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/tbaa.ttf"
    color_t = (0, 0, 0)
    size_font = 80
    codx = "tbaa_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def tbaa_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/tbaa.ttf"
    color_t = (250, 250, 250)
    size_font = 80
    codx = "tbaa_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hsha_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hsha.ttf"
    color_t = (0, 0, 0)
    size_font = 70
    codx = "hsha_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hsha_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hsha.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    codx = "hsha_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qyass_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qyasee.ttf"
    color_t = (0, 0, 0)
    size_font = 90
    codx = "qyass_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qyass_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qyasee.ttf"
    color_t = (250, 250, 250)
    size_font = 90
    codx = "qyass_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qyass2_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qyasee2.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    codx = "qyass2_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def qyass2_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/qyasee2.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    codx = "qyass2_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hur_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hur.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    codx = " hur_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hur_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hur.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    codx = "hur_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hur2_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hur2.ttf"
    color_t = (0, 0, 0)
    size_font = 100
    codx = "hur2_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def hur2_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/hur2.ttf"
    color_t = (250, 250, 250)
    size_font = 100
    codx = "hur2_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def alanat_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/alanat.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    codx = "alanat_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def alanat_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/alanat.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    codx = "alanat_WX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def alanat2_BX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/alanat2.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    codx = "alanat2_BXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)


async def alanat2_WX(client, message):
    dir_bg = "./bg1"
    dir_font = "./fonts/alanat2.ttf"
    color_t = (250, 250, 250)
    size_font = 70
    codx = "alanat2_WXX"
    await edit1(client, dir_bg, dir_font, color_t, size_font, message)
    await MOR(client, codx, message)
